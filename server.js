const express = require('express');const app = express();const http = require('http').createServer(app);const io = require('socket.io')(http);const PASSWORD="1234";app.use(express.static(__dirname));app.use(express.json());app.post('/login',(req,res)=>{res.json({ok:req.body.pass===PASSWORD});});io.on('connection',socket=>{socket.on('cmd',cmd=>{const out={SCAN:"[OK] Network scan complete",MONITOR:"[OK] Traffic monitor active",TWIN:"[SIM] Evil twin demo running",REPORT:"[OK] Security report generated",SHUT:"[SYS] Node shutting down..."};setTimeout(()=>{io.emit('status',out[cmd]||"[ERR] Unknown cmd");},600);});});http.listen(3000,()=>console.log("Server running"));